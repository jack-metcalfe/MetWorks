// Elements.Initializer
// {{GeneratedHeader}}
#nullable enable

namespace {{GeneratedNamespace}}
{
    // The ElementsInitializer builds element-driven instances.
    // All element expressions are fully computed by the pipeline.
    internal static partial class {{NamedInstanceName}}_ElementsInitializer
    {
        public static {{NamedInstanceQualifiedClassName}} CreateElements({{RegistryClassName}} registry)
        {
            // Construct the instance using element-driven values.
            return {{{ElementsConstructionExpression}}};
        }
    }
}
