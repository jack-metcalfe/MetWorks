// PrimitiveArray.Member
// Per-element add lines for primitive array {{NamedInstanceName}}
// {{GeneratedHeader}}
#nullable enable
/* Provenance: {{Provenance}} */

namespace {{Namespace}}
{
    // Partial class for primitive array members.
    // This fragment contributes per-element snippets to the {{NamedInstanceName}}_PrimitiveArrays aggregate.
    // Token contract: NamedInstanceName; Provenance; ElementType
    // MembersFragment is pipeline-supplied (constants, literals, or loop-provided fragments).
    internal static partial class {{NamedInstanceName}}_PrimitiveArray_Partial
    {
        // Member snippet for element type {{ElementType}}.
        {{MembersFragment}}
    }
}
